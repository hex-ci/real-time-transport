# ==========================================
# 前端配置 (Vite)
# ==========================================
# 前端 Web 绑定地址（默认 127.0.0.1，如需局域网/手机真机调试可配为 0.0.0.0）
VITE_HOST=127.0.0.1
# 前端 Web 端口
VITE_PORT=5173
# 明确指定后端代理目标端口（可选，默认自动跟随下方 PORT）
# VITE_SERVER_PORT=3000

# ==========================================
# 后端配置 (Fastify & Node.js)
# ==========================================
PORT=3000
HOST=0.0.0.0
LOG_LEVEL=info

# 数据库 (PostgreSQL 18, 容器默认暴露端口 5432)
DATABASE_URL=postgres://transit:transit_password@localhost:5432/transit

# WebSocket
WS_PATH=/ws

# 交通数据源配置
CHELAILE_ENABLED=true
# CHELAILE_SIGN_SALT=
# CHELAILE_AES_KEY=
APIZERO_ENABLED=false
APIZERO_KEY=
AMAP_MAPS_API_KEY=

# 活跃线路服务端轮询间隔（秒）
TRANSIT_POLL_INTERVAL_SEC=18
